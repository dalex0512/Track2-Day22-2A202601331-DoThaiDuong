# ── LangChain core ────────────────────────────────────────────────
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
# RAGAS 0.4 imports the legacy VertexAI compatibility module removed in 0.4.x.
langchain-community>=0.3.31,<0.4.0
langchain-text-splitters>=1.0.0,<2.0.0

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=1.0.0,<1.6.0          # OpenAI + OpenRouter
langchain-anthropic>=1.0.0,<2.0.0       # Anthropic Claude
langchain-google-genai>=4.0.0,<5.0.0    # Google Gemini
langchain-ollama>=1.0.0,<2.0.0          # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.2.0,<1.0.0

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas>=0.4.0,<0.5.0
datasets>=4.0.0,<5.0.0           # required by RAGAS internally
huggingface-hub>=0.25.0,<1.0.0   # compatible click range with Guardrails
numpy>=1.25.0,<3.0.0

# ── Guardrails AI ─────────────────────────────────────────────────
guardrails-ai>=0.10.0,<0.11.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=2.0.0,<3.0.0             # OpenAI SDK (compatible with Guardrails)
tiktoken>=0.5.0                  # token counting
